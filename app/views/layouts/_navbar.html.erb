<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% navbar_items.each do |menu_item| %>
          <li class="nav-item">
            <a class="nav-link" href="<%= menu_item[:path] %>"><%= menu_item[:name] %></a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
